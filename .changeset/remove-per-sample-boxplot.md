---
"@platforma-open/milaboratories.humanization-score.model": patch
"@platforma-open/milaboratories.humanization-score.ui": patch
---

Remove the per-sample box plot ("By Sample") view: the `perSamplePf` /
`perSamplePfPcols` outputs, the `graphStateBoxplot` state and its defaults, and
the `/by-sample` section are gone. The score distribution histogram remains.
